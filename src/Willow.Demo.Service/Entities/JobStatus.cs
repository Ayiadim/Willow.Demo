namespace Willow.Demo.Service.Entities
{
    /*
     * Note: Use this in Job and map the conversion in AutoMapper.
     */
    public enum JobStatus
    {
        NotStarted,
        InProgress,
        Delayed,
        Completed
    }
}
